# Generic single-database configuration

## Initial migration

```Shell
alembic revision --autogenerate -m "Initial migration"
alembic upgrade head
```

## Successive migrations

```Shell
alembic revision --autogenerate -m "Reason for change"
alembic upgrade head
```
